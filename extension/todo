V2=======================
Allow search	OK
    Create a JSON object with the url and the title while loading the tabs	OK
	Add the keywords property fetched from tab data
Selection mode when clicked on icon		OK
Load a selection of tabs	OK
	few		OK
Save a selection of tabs
	few 
Allow deletion
	Add a selection bar with these options
Protect from deletion
Allow to move the items
add a square aside when item hovered with a list of options aside
Write date in "time ago" correctly	OK
Clean the directories	OK
Propose global actions according to the selection
Allow select allow when in search mode
Remove all etc.

Bug fixes=================
Link chrome-extension or moz-extension by eventListeners 
Only delete the tab not hidden when Open all	OK
	Selection mode behavior
The selection mode doesn't go back to false when all selected current tabs are closed	OK
When selection is fast certain elements can be skipped and not selected
We can't selected/unselected more then 3 times, its bugs then
We can't select from a tab group to another tab group

Ideas for feature=========
Reorganise the element with the help of JavaScript

Give a name to a tabGroup according to the most present domain/keyword/search
antiDDOS feature limit the speed the tabs open when they are in the same domain