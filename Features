Iteration 1
Detect the URL of each tabs	OK
Make version of Keep Tabs main page UI Ok
	Add a save button	OK 
Save tabs somewhere
	Make previous code cleaner and comment
	Create a JSON from current tabs	OK
	Save the JSON in a file		
	Add the secret mode for tabSet
	Launch a render function when ended
Render saved tabs
	Make previous code cleaner and comment
	Generate a list from a JSON
	Make the list savable
Close all saved tabs
	Make previous code cleaner and comment
	Loop and close all tabs

Load all saved tabds
Load one tab
Load a selection of tabs
Delete while loaded

Active research: Filter instantaneously while typing
Mood system
	Reorder into moods
	Define secret and non-secret moods
Load tabs with delay between

Load in another window

Create the in context sidebar

Protect mode for tabs/tab sections allow the items to not be deleted
Make keeptabs as homepage 
Add the posibility to search on the internet ?

Iteration 2
Allow delete
Allow to move the items
add a square aside when item hovered with a list of options aside

Iteration 3
Allow search
Allow sorting

Iteration 4
Allow auto-delete = pb parfois videos d'info
->Replace by auto-hide with a show hidden tabs button

Iteration U1
Link the tabs to the Firefox Account
Save a image of the URL in thumbnail mode

Possibility to add contextual actions such as YouTube AutoPlay list
Support for multi-window

Don't load twice a same favicon
